import 'package:flutter/material.dart';
import 'layoutUsuaria.dart'; // Importa el LayoutUsuaria existente

class Emprendimientos extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        LayoutUsuaria(),
      ],
    );
  }
}
